import './styling/groups.css'

export default function Groups () {
  return (
    <div className="group-component">Show groups component Here we can show the list of all groups. When we click it goest to a OneGroup component</div>
  )
}